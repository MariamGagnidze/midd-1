const readFile = require("./readFile");
const writeFile = require("./writeFile");
const currentTime = require("./time");

module.exports = {
  readFile: readFile,
  writeFile: writeFile,
  currentTime,
};
